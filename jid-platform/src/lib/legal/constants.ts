/** Section 8 — legal document version metadata (shared across privacy & terms). */
export const LEGAL_DOCUMENT_VERSION = '1.0' as const

/** Effective date for v1.0 (YYYY-MM-DD). */
export const LEGAL_EFFECTIVE_DATE = '2026-07-07' as const
